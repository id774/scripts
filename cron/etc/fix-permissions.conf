########################################################################
# fix-permissions.conf
#
#  This file is sourced by the calling script to adjust ownership and
#  permissions on common system paths and cron directories.
#  Includes selected application paths, cron directories,
#  firewall rules file, and TCP Wrappers configuration files.
#
#  Rules:
#  - Blank lines are ignored.
#  - Lines starting with '#' are treated as comments (leading spaces allowed).
#  - This file must be POSIX sh compatible.
#  - Do not add a shebang; this file is sourced, not executed.
#
#  Variables:
#  - ADMIN_MAIL_ADDRESS: Optional administrative email address.
#    Example (disabled by default):
#    #ADMIN_MAIL_ADDRESS="root"
#
#  Functions:
#  - fix_permissions: Perform ownership and mode adjustments on specific
#    system directories.
#
#  Symbolic Link Ownership:
#  - chmodtree excludes symbolic links from --user/--group normalization by
#    default. --chown-symlinks is added only for /opt/python, /opt/ruby,
#    /usr/local/etc, and /usr/local/src, since these local trees may contain
#    intentional version-switch symlinks (e.g. /opt/python/current) that this
#    script is expected to keep normalized to root:root.
#  - Other targets below intentionally omit --chown-symlinks and rely on the
#    default exclusion, since symbolic links are not part of their expected
#    layout.
#
#  Logging:
#  - Expects JOBLOG to be defined by the caller. All actions append to JOBLOG.
#
########################################################################

# Define admin mail address
#ADMIN_MAIL_ADDRESS="root"

# Define fix_permissions as a configurable operation
fix_permissions() {
    log_info_time "Setting permission for /opt/python, /opt/ruby"
    for dir in /opt/python /opt/ruby; do
        [ -d "$dir" ] || continue
        chmodtree -q --user root --group root --chown-symlinks "$dir"
    done

    log_info_time "Setting permission for /usr/local/etc, /usr/local/src"
    chmodtree -q --user root --group root --chown-symlinks /usr/local/etc
    chmodtree -q --user root --group root --chown-symlinks /usr/local/src

    log_info_time "Setting permission for /var/log/chkrootkit"
    chmodtree -q --user root --group adm /var/log/chkrootkit
    chmod 0750 /var/log/chkrootkit

    log_info_time "Setting permission for /etc/cron.*"
    for dir in /etc/cron.hourly /etc/cron.daily /etc/cron.weekly /etc/cron.monthly /etc/cron.yearly /etc/cron.weekday /etc/cron.weekend /etc/cron.exec; do
        [ -d "$dir" ] || continue
        chmodtree -q --user root --group adm -f 0740 "$dir"
    done
    for dir in /etc/cron.d /etc/cron.config; do
        [ -d "$dir" ] || continue
        chmodtree -q --user root --group adm -f 0640 "$dir"
    done

    log_info_time "Setting permission for /etc/iptables/rules.v4"
    if [ -f /etc/iptables/rules.v4 ]; then
        chmodtree -q --user root --group root -f 0400 /etc/iptables/rules.v4
    fi

    log_info_time "Setting permission for /etc/hosts.allow, /etc/hosts.deny"
    for file in /etc/hosts.allow /etc/hosts.deny; do
        [ -f "$file" ] || continue
        chmodtree -q --user root --group adm -f 0640 "$file"
    done
}
